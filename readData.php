<?php
$myfile = fopen("Data.cfg", "r") or die("Unable to open file!");

while(!feof($myfile)) {
	
 //fgets($myfile)."<br/>";

 $currentStr=fgets($myfile);
 //Processing starts here
 if($)
 
}

fclose($myfile);
?> 